import 'package:flutter/material.dart';

class PickleScreen extends StatelessWidget {
  const PickleScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
