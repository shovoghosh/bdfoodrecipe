import 'package:flutter/material.dart';

class DalScreen extends StatelessWidget {
  const DalScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
