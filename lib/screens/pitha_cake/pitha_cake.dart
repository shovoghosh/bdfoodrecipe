import 'package:flutter/material.dart';

class PithaCakeScreen extends StatelessWidget {
  const PithaCakeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
